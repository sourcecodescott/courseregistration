<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.course_registration">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".resetUserPassword"
            android:label="@string/title_activity_reset_user_password"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".ProgramRequirementsActivity"
            android:label="@string/title_activity_program_requirements"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".StudentScheduleList" />
        <activity android:name=".view_course_list" />
        <activity android:name=".ViewCourseDetail" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ContactViewHolder" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>